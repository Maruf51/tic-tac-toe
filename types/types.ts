export interface PlayerCpuScoreTypes {
    player: number,
    cpu: number,
    ties: number
}

export interface PlayersScoreTypes {
    player1: number,
    player2: number,
    ties: number
}

export interface FieldTypes {
    player: boolean | null,
    id: number
}